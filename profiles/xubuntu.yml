---
# Xubuntu 24.04 on a Proxmox VM — full Docker Engine, XRDP, SSH server.
docker_install: true
docker_daemon: true
docker_apt_repo_url: "https://download.docker.com/linux/ubuntu"
install_vscode: true
install_claude_code: true
clone_repos: false

# Consumed by the remote-desktop / power roles (follow-up PR).
desktop_env: xfce
enable_xrdp: true
enable_tlp: false

base_extra_packages:
  - openssh-server
  - qemu-guest-agent
base_services:
  - ssh
  - qemu-guest-agent
