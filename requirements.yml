---
# Galaxy collections required by the roles. Installed by bootstrap.sh and the
# ansible-lint CI workflow via: ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general
    version: ">=8.0.0"
