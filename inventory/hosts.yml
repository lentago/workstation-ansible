---
# Self-provisioning: the target is always the machine running the play.
# Profile selection (crostini|xubuntu|fedora|ubuntu_laptop) is by variable, not
# by inventory group — see site.yml's autodetect/override logic.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
